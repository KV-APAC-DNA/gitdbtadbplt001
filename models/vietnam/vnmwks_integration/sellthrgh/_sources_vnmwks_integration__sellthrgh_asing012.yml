version: 2

sources:
  - name: vnmwks_integration
    database: "{{ env_var('DBT_ENV_CORE_DB') }}"
    schema: vnmwks_integration
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: wrk_vn_mnth_week
        tags: ["vn_dms_sellthrough_weekly_target","ingestion"]