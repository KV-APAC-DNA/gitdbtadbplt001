version: 2

sources:
  - name: snaposeitg_integration
    database: "{{ env_var('DBT_ENV_CORE_DB') }}"
    schema: snaposeitg_integration
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: itg_vn_mt_dksh_product_master
      - name: itg_vn_mt_sellin_target