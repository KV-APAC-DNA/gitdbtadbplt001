version: 2

sources:
  
  - name: aspitg_integration
    database: "{{ env_var('DBT_ENV_CORE_DB') }}"
    schema: aspitg_integration
    quoting:
      database: false
      schema: false
      identifier: false

  # disabling here as this is available main branch in (models/asp_regional/aspitg_integration/mds/_sources_aspitg_integration__mds.yml) file
    #tables:
     # - name: itg_mds_master_mother_code_mapping

  - name: chnedw_integration
    database: "{{ env_var('DBT_ENV_CORE_DB') }}"
    schema: chnedw_integration
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
     
      - name: edw_perfect_store_product_master
