version: 2

sources:
  - name: myswks_integration
    database: "{{ env_var('DBT_ENV_CORE_DB') }}"
    schema: myswks_integration
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: wks_my_base
