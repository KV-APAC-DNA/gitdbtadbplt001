version: 2

models:
  - name: idnitg_integration__sdl_raw_stock_dist_map
    config:
      alias: sdl_raw_stock_dist_map
      tags: ["id_stock_load","ingestion"]
  - name: idnitg_integration__itg_stock_dist_map
    config:
      alias: itg_stock_dist_map
      tags: ["id_stock_load","ingestion"]