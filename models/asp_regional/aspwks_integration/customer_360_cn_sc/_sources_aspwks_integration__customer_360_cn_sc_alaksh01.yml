version: 2

sources:
  - name: chnitg_integration
    database: "{{ env_var('DBT_ENV_CORE_DB') }}"
    schema: chnitg_integration
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: itg_cn_selfcare_sellout_fact
        tags: ["customer_360_cn_sc","ingestion"]
