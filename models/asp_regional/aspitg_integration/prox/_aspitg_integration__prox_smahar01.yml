version: 2

models:
  - name: aspitg_integration__itg_prox_sys_department
    config:
      alias: itg_prox_sys_department
      tags: ["prox_department","ingestion"]
  - name: aspitg_integration__sdl_raw_prox_sys_department
    config:
      alias: sdl_raw_prox_sys_department
      tags: ["prox_department","ingestion"]
  - name: aspitg_integration__itg_prox_report_bi_budgetusage
    config:
      alias: itg_prox_report_bi_budgetusage
      tags: ["prox_budget","ingestion"]
  - name: aspitg_integration__sdl_raw_prox_report_bi_budgetusage
    config:
      alias: sdl_raw_prox_report_bi_budgetusage
      tags: ["prox_budget","ingestion"]
  - name: aspitg_integration__itg_prox_report_bi_contract
    config:
      alias: itg_prox_report_bi_contract
      tags: ["prox_contract","ingestion"]
  - name: aspitg_integration__sdl_raw_prox_report_bi_contract
    config:
      alias: sdl_raw_prox_report_bi_contract
      tags: ["prox_contract","ingestion"]
  - name: aspitg_integration__itg_prox_report_bi_payment
    config:
      alias: itg_prox_report_bi_payment
      tags: ["prox_payment","ingestion"]
  - name: aspitg_integration__sdl_raw_prox_report_bi_payment
    config:
      alias: sdl_raw_prox_report_bi_payment
      tags: ["prox_payment","ingestion"]
  
  