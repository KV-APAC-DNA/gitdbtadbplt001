sources:
  - name: phlsdl_raw
    database: "{{ env_var('DBT_ENV_LOAD_DB') }}"
    schema: phlsdl_raw
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: sdl_pos_rks_rose_pharma
        tags: ["ph_rks_rose_pharma","ingestion"]

  - name: phlitg_integration
    database: "{{ env_var('DBT_ENV_CORE_DB') }}"
    schema: phlitg_integration
    quoting:
      database: false
      schema: false
      identifier: false
    tables:
      - name: itg_mds_ph_pos_rosepharma_products
      - name : itg_mds_ph_pos_rosepharma_customers